spring:
  application:
    name: ms-user
  cloud:
    kubernetes:
      config:
        sources:
          - name: ${spring.application.name}
      discovery:
        all-namespaces: true
      loadbalancer:
        mode: service
      reload:
        mode: polling
        period: 10000
      enabled: false
