spring:
  application:
    name: ms-user
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 1000
      config:
        sources:
          - name: ${spring.application.name}
      discovery:
        all-namespaces: true
